# nnUNet Server

FastAPI-based backend for managing nnU-Net model inference and SLURM job handling.

## Run locally
\`\`\`bash
uvicorn app.main:app --reload
\`\`\`

## Run in Docker
\`\`\`bash
docker-compose up --build
\`\`\`
# nnunet_server
# nnunet_server
